-- 1. Gagawa ng Database
CREATE DATABASE IF NOT EXISTS healthcare_db;
USE healthcare_db;

-- 2. Gagawa ng Table para sa Users
CREATE TABLE IF NOT EXISTS users (
    id INT(11) AUTO_INCREMENT PRIMARY KEY,
    fname VARCHAR(50) NOT NULL,
    lname VARCHAR(50) NOT NULL,
    contact_num VARCHAR(15) NOT NULL,	
    gender VARCHAR(10) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    birth_month VARCHAR(20) NOT NULL,
    birth_day INT(2) NOT NULL,
    birth_year INT(4) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


ALTER TABLE users ADD COLUMN expertise VARCHAR(50) NOT NULL AFTER email;

ALTER TABLE users ADD COLUMN profile_pix VARCHAR(255) DEFAULT NULL;